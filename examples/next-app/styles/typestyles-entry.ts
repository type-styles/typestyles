/**
 * Side-effect entry for `buildTypestylesForNext`: load the shared design system
 * first, then register this app’s layout/shell styles.
 */
import '@examples/react-design-system';
import './site';
