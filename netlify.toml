[build]
  command = "turbo run build --filter docs"
  publish = "docs/dist"

[functions]
  directory = "docs/netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/mcp"
  to = "/.netlify/functions/mcp"
  status = 200
