---
'typestyles': minor
---

Add `@property` registration for typed token leaves (`{ value, syntax, inherits? }` in `tokens.create`) and standalone `styles.property(id, opts)`. Both return `{ name, var, toString }` refs (P3.24).
