---
'typestyles': minor
---

Add `styles.breakpoint()` and `styles.media()` helpers that build viewport `@media` keys from configured `breakpoints`, matching the `container()` + `atRuleBlock` pattern for container queries.
