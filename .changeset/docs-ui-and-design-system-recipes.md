---
---

Expand the Astro documentation layout and components: table-of-contents column and navigation, skip link, pagination, doc footer, and reusable content primitives (aside, badge, card grid, link cards, file tree, steps).

Add matching typestyles recipes to `@examples/design-system` (badge, card, steps, file tree) and align docs styles and tokens with those patterns.

**Workspace-only:** `typestyles-docs` and `@examples/design-system` are ignored by Changesets; no published npm packages are versioned by this entry.
