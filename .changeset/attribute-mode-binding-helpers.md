---
'typestyles': minor
---

Add `mergeProps` and `combine` helpers for attribute-mode DOM binding, and dev warnings when `cx()` drops variant attrs. Raise main-entry gzip budget (+800 B) for the new runtime.
