---
'typestyles': patch
---

Default component `c.var()`, `c.vars()`, and `.declare()` `@property` registrations to `inherits: true`, matching `tokens.declare()` behavior. Explicit `inherits: false` overrides are preserved.
