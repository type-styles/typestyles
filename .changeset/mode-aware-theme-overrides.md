---
'typestyles': minor
---

Accept inline `{ light, dark }` token leaves in theme overrides with automatic normalization (#217). Export `normalizeModeAwareOverrides` and `normalizeThemeConfig`. Raise main-entry gzip budget (+600 B) for normalization runtime.
