---
'typestyles': minor
'@typestyles/eslint-plugin': minor
---

Add `styles.scope()` for proximity-correct nested theme overrides via CSS `@scope`, the `typestyles snapshot-classnames` CLI, and the opt-in `@typestyles/no-removed-public-classname` ESLint rule for guarding semantic class renames in publishable packages (P5.3).
