---
'typestyles': minor
---

Request-safe SSR collection (P0.4): isolate `collectStyles` / `collectStylesFromModules` sheet state with Node `AsyncLocalStorage`, and add async render support to `collectStyles`.
