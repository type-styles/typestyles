---
'typestyles': minor
---

Add `createTheme({ components })` for namespace-keyed recipe overrides on the same `createTypeStyles` instance (#216). Raise main-entry gzip budget (+400 B) for theme component override runtime.
