---
'typestyles': minor
---

Export `ModeAwareTokenLeaf`, `ModeAwareTokenObject`, and `TokenRefTree`; preserve input shape in `tokens.create()` / `tokens.extend()` return types (nested namespaces, mode-aware leaves, plain scalars). Add `tokens.extend()` as an idempotent namespace registration alias.
