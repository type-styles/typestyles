---
'typestyles': minor
---

Export `ModeAwareTokenLeaf`, `ModeAwareTokenObject`, and `TokenRefTree`; preserve input shape in `tokens.create()` return types (nested namespaces, mode-aware leaves, plain scalars).
