---
---

Add an unstable release workflow that publishes all workspace packages from `main` using commit-based prerelease versions under the `unstable` npm tag, while keeping the existing Changesets production release flow.
