# Attribute-driven variants

This historical spec has been consolidated into
[Semantic Default + Attribute Mode](./semantic-and-attribute-mode.md).

For the current attribute-mode contract — semantic base and slot classes,
kebab-cased `data-*` attributes, flat component names, and per-slot attrs results —
use that specification.
